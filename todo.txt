# parallel download

0. check autocal mbit
1. shutdown incl. postprocessor!!!! -> incl. events
2. implement pause/resume






1. postprocess hangs at terminating par_verifier sometimes(!?)
2. enable quick shutdown auf unrarer (if in the middle of unraring)
2a. if 2 nzbs -> after first download speed levelbar not updated for a longer
time (only after unrarer ist started !?)
3. pause / resume -> sometimes rar skipped etc. !!??
4. test downloaded_size after resume
5. if rars have no ending and no par2 -> check in separate textfile. SV S01E10 e.g.
6. show overall eta in gui / NZB_QUEUE
7. enable more robust shutdown of connections in case they are unstable e.g
8. implement "add nzb" feature in gtkgui / NZB_QUEUE
9. make gzbx more robust if there are no cennctions, e.g. with shutdown
10. quicker termination of nzb_parser ...
